{
  "rules": {
    ".read": false,
    ".write": false,
    "days_exercised": {
      "$user": {
        ".read": "$user === auth.uid",
        ".write": "$user === auth.uid",
        ".indexOn": "timeStamp"
      }
    },
    "exercises": {
      "$user": {
        ".read": "$user === auth.uid",
        ".write": "$user === auth.uid",
        ".indexOn": "timeStamp"
      }
    },
    "new_exercises": {
      "$user": {
        ".read": "$user === auth.uid",
        ".write": "$user === auth.uid",
        ".indexOn": "timeStamp"
      }
    }
  }
}
